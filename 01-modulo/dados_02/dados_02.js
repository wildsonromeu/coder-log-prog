// Toda variável tem seu identifcador, que é o seu nome.
// É importante definir bons nomes para pastas, arquivos e variáveis.
// Posso atribuir um determinado valor para variável ou não.

let preco = 19.90;
let desconto = 0.4;
let precoComDesconto = preco * (1-desconto);

console.log("Desconto = " + preco * desconto);
console.log("Valor com desconto aplicado = " + precoComDesconto);